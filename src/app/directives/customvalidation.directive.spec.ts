import { CustomvalidationDirective } from './customvalidation.directive';

describe('CustomvalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomvalidationDirective();
    expect(directive).toBeTruthy();
  });
});
